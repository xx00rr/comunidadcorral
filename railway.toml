[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && python manage.py create_admin && gunicorn comunidadcorral.wsgi --log-file -"
healthcheckPath = "/login/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
